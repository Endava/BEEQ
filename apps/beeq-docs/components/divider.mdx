---
title: Divider
description: A divider visually separates related content so users can scan layout groups, sections, and lists more easily.
---

import { CardTile } from '/snippets/CardTile.jsx';
import { CodeLivePreview } from "/snippets/CodeLivePreview.jsx";

<Frame className="px-4 py-4" caption="Divider overview">
  <img
    src="https://placeholdit.com/600x400"
    alt="Placeholder for the Divider overview image"
    style={{ height: "200px", width: "400px" }}
  />
</Frame>

A divider creates a visual break between related pieces of content. Use it to clarify grouping without adding another heading, card, or container.

## When to use

<CardGroup cols={2}>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="thumbs-up" iconType="solid" size={20} color="var(--bq-stroke--success)" />
      Use dividers when
    </span>
    - Content groups need a subtle visual boundary
    - A list or page section needs clearer scanning
    - Related items sit close together and spacing alone is not enough
    - Horizontal or vertical separation improves the layout hierarchy
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="thumbs-down" iconType="solid" size={20} color="var(--bq-stroke--danger)" />
      Do not use dividers when
    </span>
    - Whitespace already separates the content clearly
    - The divider adds clutter without improving comprehension
    - A heading, card, or layout change would explain the grouping better
    - Users need a semantic landmark or navigational structure
  </Card>
</CardGroup>

## Anatomy

<Frame className="px-4 py-4" caption="Divider anatomy">
  <img
    src="https://placeholdit.com/1000x320"
    alt="Placeholder for the Divider anatomy image"
    className="h-[260px] w-full object-cover"
  />
</Frame>

| Part | Element | Description |
|---|---|---|
| **1** | Stroke | The visual line that separates content |
| **2** | Title | Optional content placed in the default slot |
| **3** | Start segment | The line segment before the title |
| **4** | End segment | The line segment after the title |

## Design guidelines

### Types

<CardGroup cols={2}>
  <CardTile
    title="Horizontal divider"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Use horizontal dividers to separate stacked content, sections, list rows, or blocks inside a page.
  </CardTile>
  <CardTile
    title="Vertical divider"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Use vertical dividers between side-by-side controls or columns when spacing alone does not show the relationship clearly.
  </CardTile>
  <CardTile
    title="Dashed divider"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Use dashed dividers for lighter separation or secondary grouping. Keep the pattern subtle so it does not compete with content.
  </CardTile>
  <CardTile
    title="Divider with title"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Add title content when users need a short label for the group that follows.
  </CardTile>
</CardGroup>

### Placement

<CardGroup cols={2}>
  <CardTile
    title="Between sections"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Place dividers between groups that belong to the same surface but need clearer structure.
  </CardTile>
  <CardTile
    title="Inside dense lists"
    imageLightSrc="https://placeholdit.com/600x400"
    imageDarkSrc="https://placeholdit.com/600x400"
  >
    Use dividers in dense lists when repeated items need stronger row boundaries for scanning.
  </CardTile>
</CardGroup>

### Sizing

<Note>
  Divider strokes default to a thin line. Increase `stroke-thickness` only when the divider must match a stronger visual treatment in the surrounding layout.
</Note>

## Usage

### Default

Use the default divider to separate stacked content with a subtle horizontal line and optional title.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    inline-size: 100% !important;
  }

  .divider--default {
    inline-size: 100%;
  }

  .divider--default p {
    margin: 0;
    white-space: nowrap;
  }
}
</style>
<bq-divider class="divider--default">
  <p style="margin: 0; white-space: nowrap;">Default divider</p>
</bq-divider>`}>
  <CodeGroup>

    ```html HTML icon="html5"
    <bq-divider class="divider--default">
      <p>Default divider</p>
    </bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <BqDivider className="divider--default">
      <p>Default divider</p>
    </BqDivider>
    ```

    ```html Angular icon="angular"
    <bq-divider class="divider--default">
      <p>Default divider</p>
    </bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider class="divider--default">
        <p>Default divider</p>
      </BqDivider>
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

### Horizontal

Set `orientation="horizontal"` when the divider separates content along the vertical flow of a page.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    inline-size: 100% !important;
  }

  .divider--horizontal {
    inline-size: 100%;
  }

  .divider--horizontal p {
    margin: 0;
    white-space: nowrap;
  }
}
</style>
<bq-divider class="divider--horizontal" orientation="horizontal">
  <p>Horizontal divider</p>
</bq-divider>`}>
  <CodeGroup>

    ```html HTML icon="html5"
    <bq-divider class="divider--horizontal" orientation="horizontal">
      <p>Horizontal divider</p>
    </bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <BqDivider className="divider--horizontal" orientation="horizontal">
      <p>Horizontal divider</p>
    </BqDivider>
    ```

    ```html Angular icon="angular"
    <bq-divider class="divider--horizontal" orientation="horizontal">
      <p>Horizontal divider</p>
    </bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider class="divider--horizontal" orientation="horizontal">
        <p>Horizontal divider</p>
      </BqDivider>
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

### Vertical

Set `orientation="vertical"` when the divider separates side-by-side content, such as adjacent actions or columns.

<CodeLivePreview code={`<style>
@scope {
  .divider--vertical-example {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 120px;
    gap: var(--bq-spacing-m);
  }

  .divider--vertical-example p {
    margin: 0;
    white-space: nowrap;
  }
}
</style>
<div class="divider--vertical-example">
  <span>Left</span>
  <bq-divider orientation="vertical">
    <p>Vertical divider</p>
  </bq-divider>
  <span>Right</span>
</div>`}>
  <CodeGroup>
    ```html HTML icon="html5"
    <div class="divider--vertical-example">
      <span>Left</span>
      <bq-divider orientation="vertical">
        <p>Vertical divider</p>
      </bq-divider>
      <span>Right</span>
    </div>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <div className="divider--vertical-example">
      <span>Left</span>
      <BqDivider orientation="vertical">
        <p>Vertical divider</p>
      </BqDivider>
      <span>Right</span>
    </div>
    ```

    ```html Angular icon="angular"
    <div class="divider--vertical-example">
      <span>Left</span>
      <bq-divider orientation="vertical">
        <p>Vertical divider</p>
      </bq-divider>
      <span>Right</span>
    </div>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <div class="divider--vertical-example">
        <span>Left</span>
        <BqDivider orientation="vertical">
          <p>Vertical divider</p>
        </BqDivider>
        <span>Right</span>
      </div>
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

## Options

### Dashed

Use `dashed` for a lighter visual boundary. Adjust `stroke-dash-width`, `stroke-dash-gap`, and `stroke-linecap` when the dash pattern needs to match the surrounding interface.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    inline-size: 100% !important;
  }
}
</style>
<bq-divider
  dashed
  stroke-dash-width="12"
  stroke-dash-gap="7"
  stroke-linecap="butt"
></bq-divider>`}>
  <CodeGroup>
    ```html HTML icon="html5"
    <bq-divider
      dashed
      stroke-dash-width="12"
      stroke-dash-gap="7"
      stroke-linecap="butt"
    ></bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <BqDivider
      dashed
      strokeDashWidth={12}
      strokeDashGap={7}
      strokeLinecap="butt"
    />
    ```

    ```html Angular icon="angular"
    <bq-divider
      dashed
      stroke-dash-width="12"
      stroke-dash-gap="7"
      stroke-linecap="butt"
    ></bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider
        dashed
        :strokeDashWidth="12"
        :strokeDashGap="7"
        strokeLinecap="butt"
      />
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

### No title

Omit title content when the divider only needs to separate sections visually.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    inline-size: 100% !important;
  }
}
</style>
<bq-divider></bq-divider>`}>
  <CodeGroup>
    ```html HTML icon="html5"
    <bq-divider></bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <BqDivider />
    ```

    ```html Angular icon="angular"
    <bq-divider></bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider />
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

### Title alignment

Use `title-alignment` to place title content at the start, middle, or end of the divider.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    gap: var(--bq-spacing-l) !important;
    inline-size: 100% !important;
  }

  bq-divider p {
    margin: 0;
    white-space: nowrap;
  }
}
</style>
<bq-divider title-alignment="start">
  <p>Text start</p>
</bq-divider>
<bq-divider title-alignment="middle">
  <p>Text middle</p>
</bq-divider>
<bq-divider title-alignment="end">
  <p>Text end</p>
</bq-divider>`}>
  <CodeGroup>
    ```css CSS icon="css"
    bq-divider {
      & p {
        margin: 0;
        white-space: nowrap;
      }
    }
    ```

    ```html HTML icon="html5"
    <bq-divider title-alignment="start">
      <p>Text start</p>
    </bq-divider>
    <bq-divider title-alignment="middle">
      <p>Text middle</p>
    </bq-divider>
    <bq-divider title-alignment="end">
      <p>Text end</p>
    </bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <>
      <BqDivider titleAlignment="start">
        <p>Text start</p>
      </BqDivider>
      <BqDivider titleAlignment="middle">
        <p>Text middle</p>
      </BqDivider>
      <BqDivider titleAlignment="end">
        <p>Text end</p>
      </BqDivider>
    </>
    ```

    ```html Angular icon="angular"
    <bq-divider title-alignment="start">
      <p>Text start</p>
    </bq-divider>
    <bq-divider title-alignment="middle">
      <p>Text middle</p>
    </bq-divider>
    <bq-divider title-alignment="end">
      <p>Text end</p>
    </bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider titleAlignment="start">
        <p>Text start</p>
      </BqDivider>
      <BqDivider titleAlignment="middle">
        <p>Text middle</p>
      </BqDivider>
      <BqDivider titleAlignment="end">
        <p>Text end</p>
      </BqDivider>
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

### Stroke

Use stroke attributes when the divider needs to match a specific visual treatment.

<CodeLivePreview code={`<style>
@scope {
  :scope {
    align-items: stretch !important;
    inline-size: 100% !important;
  }
}
</style>
<bq-divider
  stroke-color="stroke--brand"
  stroke-thickness="2"
  stroke-basis="96"
  title-alignment="start"
>
  Section
</bq-divider>`}>
  <CodeGroup>
    ```html HTML icon="html5"
    <bq-divider
      stroke-color="stroke--brand"
      stroke-thickness="2"
      stroke-basis="96"
      title-alignment="start"
    >
      Section
    </bq-divider>
    ```

    ```jsx React icon="react"
    import { BqDivider } from "@beeq/react";

    <BqDivider
      strokeColor="stroke--brand"
      strokeThickness={2}
      strokeBasis={96}
      titleAlignment="start"
    >
      Section
    </BqDivider>
    ```

    ```html Angular icon="angular"
    <bq-divider
      stroke-color="stroke--brand"
      stroke-thickness="2"
      stroke-basis="96"
      title-alignment="start"
    >
      Section
    </bq-divider>
    ```

    ```vue Vue icon="vuejs"
    <script setup lang="ts">
    import { BqDivider } from "@beeq/vue";
    </script>

    <template>
      <BqDivider
        strokeColor="stroke--brand"
        :strokeThickness="2"
        :strokeBasis="96"
        titleAlignment="start"
      >
        Section
      </BqDivider>
    </template>
    ```
  </CodeGroup>
</CodeLivePreview>

## Best practices

<CardGroup cols={2}>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="check" iconType="solid" size={20} color="var(--bq-stroke--success)" />
      Do
    </span>
    Use dividers to clarify groups that already belong together.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="xmark" iconType="solid" size={20} color="var(--bq-stroke--danger)" />
      Don't
    </span>
    Do not use dividers to force unrelated content into the same section.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="check" iconType="solid" size={20} color="var(--bq-stroke--success)" />
      Do
    </span>
    Prefer spacing first, then add a divider when the boundary still needs emphasis.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="xmark" iconType="solid" size={20} color="var(--bq-stroke--danger)" />
      Don't
    </span>
    Do not add repeated dividers that make the layout feel busy.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="check" iconType="solid" size={20} color="var(--bq-stroke--success)" />
      Do
    </span>
    Use title content only when the label helps users understand the next group.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="xmark" iconType="solid" size={20} color="var(--bq-stroke--danger)" />
      Don't
    </span>
    Do not use divider title content as a replacement for a real heading.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="check" iconType="solid" size={20} color="var(--bq-stroke--success)" />
      Do
    </span>
    Keep stroke color and thickness consistent with the surrounding hierarchy.
  </Card>
  <Card>
    <span className="flex items-center mr-2 text-lg font-medium" role="heading">
      <Icon className="mr-2" icon="xmark" iconType="solid" size={20} color="var(--bq-stroke--danger)" />
      Don't
    </span>
    Do not rely on color or line style as the only way to communicate meaning.
  </Card>
</CardGroup>

## Accessibility

- Divider strokes are rendered as SVG lines with `aria-hidden="true"`, so assistive technologies ignore the decorative line.
- Content passed into the default slot remains available to assistive technologies.
- Use slot content only for short labels that help identify the following group.
- Do not rely on a divider alone to communicate page structure. Use headings, landmarks, or list semantics when users need navigation or hierarchy.
- Keep vertical dividers out of the keyboard focus order unless nearby interactive content already provides a clear path.

## API reference

### Properties

| Property | Attribute | Description | Type | Default |
|---|---|---|---|---|
| `dashed` | `dashed` | If `true`, the divider uses a dashed pattern | `boolean` | `false` |
| `orientation` | `orientation` | The divider orientation | `'horizontal' \| 'vertical'` | `'horizontal'` |
| `strokeBasis` | `stroke-basis` | Minimum stroke width when title content is not centered, in pixels | `number` | `0` |
| `strokeColor` | `stroke-color` | Stroke color token from the palette | `string` | `'stroke--primary'` |
| `strokeDashGap` | `stroke-dash-gap` | Gap between dashes when `dashed` is set | `number` | `7` |
| `strokeDashWidth` | `stroke-dash-width` | Width of each dash when `dashed` is set | `number` | `12` |
| `strokeLinecap` | `stroke-linecap` | Line cap style when `dashed` is set | `'butt' \| 'round' \| 'square'` | `'butt'` |
| `strokeThickness` | `stroke-thickness` | Stroke thickness, in pixels | `number` | `1` |
| `titleAlignment` | `title-alignment` | Alignment of title content on the divider axis | `'end' \| 'middle' \| 'start'` | `'middle'` |

### Slots

| Slot | Description |
|---|---|
| *(default)* | Optional title content displayed between divider strokes |

### Shadow parts

| Part | Description |
|---|---|
| `base` | The component's internal wrapper |
| `dash-end` | The internal SVG wrapper for the end stroke |
| `dash-end-line` | The line element for the end stroke |
| `dash-start` | The internal SVG wrapper for the start stroke |
| `dash-start-line` | The line element for the start stroke |

### CSS custom properties

<Expandable title="CSS variables" defaultOpen={true}>
  | Variable | Description | Default |
  |---|---|---|
  | `--bq-divider--color` | Divider color | `theme(stroke.primary)` |
  | `--bq-divider--title-marginX` | Space between title content and delimiter strokes | `theme(spacing.m)` |
</Expandable>

<Tip>
  Learn more about [styling with shadow parts](/theming/styles) and [CSS custom properties](/theming/global-css-variables).
</Tip>

## Resources

<CardGroup cols={2}>
  <Card horizontal title="Interactive playground" icon="code" href="https://storybook.beeq.design/?path=/story/components-divider--default">
    Explore divider variants and states in Storybook
  </Card>
  <Card horizontal title="Source code" icon="github" href="https://github.com/Endava/BEEQ/tree/main/packages/beeq/src/components/divider">
    View the component source on GitHub
  </Card>
</CardGroup>
