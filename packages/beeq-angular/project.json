{
  "name": "beeq-angular",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/beeq-angular/src",
  "projectType": "library",
  "tags": ["wrapper", "angular", "publishable"],
  "prefix": "beeq",
  "implicitDependencies": ["beeq"],
  "targets": {
    "prebuild": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/src/directives", "{projectRoot}/standalone/src/directives"],
      "options": {
        "commands": ["tsx packages/beeq-angular/scripts/patch-value-accessor-event-targets.ts"]
      },
      "dependsOn": [{ "projects": "beeq", "target": "build-stencil" }]
    },
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/beeq-angular"],
      "options": {
        "project": "{projectRoot}/ng-package.json",
        "tsConfig": "{projectRoot}/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "{projectRoot}/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "{projectRoot}/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production",
      "dependsOn": [{ "projects": "beeq", "target": "build-stencil" }, { "target": "prebuild" }]
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec biome check {projectRoot}"
      }
    }
  }
}
